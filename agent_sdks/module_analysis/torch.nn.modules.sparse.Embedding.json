{
  "class_name": "torch.nn.modules.sparse.Embedding",
  "kernels": [
    {
      "kernel_type": "direct_operation",
      "operation": "Embedding lookup operation",
      "analysis": "The embedding layer performs a lookup operation where each index in the input tensor retrieves the corresponding row from the weight matrix. Input has shape (batch_size, seq_len, ...) with total num_elements indices. Weight matrix has shape (num_embeddings, embedding_dim). Output has shape (batch_size, seq_len, ..., embedding_dim). This is a memory-bound operation with no significant FLOPs.",
      "flops": "0",
      "memory_access": {
        "read": "{num_elements} * (4 + {embedding_dim} * {w_bytes})",
        "write": "{num_elements} * {embedding_dim} * {a_bytes}"
      }
    }
  ]
}